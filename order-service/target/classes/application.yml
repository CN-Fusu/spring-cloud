server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_order?useSSL=false
    username: root
    password: "041122"
    driver-class-name: com.mysql.jdbc.Driver
  application:
      name: orderservice
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        cluster-name:
mybatis:
  type-aliases-package: cn.itcast.user.pojo
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn.itcast: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS


#eureka:
#  client:
#    service-url:
#      defaultZone: http://127.0.0.1:8761/eureka


ribbon:
  eager-load:
    enabled: true  #开启饥饿加载
    clients:  #指定客户端名称,这个是一个集合类型 如果有多个客户端可以写多个 以-的形式
      - userservice